# Copy to .env and fill in provider credentials.
ENVIRONMENT=development
# DEFAULT_PROVIDER=echo  # Optional: if unset, provider must be prefixed per request
GATEWAY_TIMEOUT_SECONDS=30

OPENAI_KEY=
GEMINI_KEY=
CLAUDE_KEY=
