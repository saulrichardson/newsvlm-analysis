You are auditing whether the CURRENT extracted ordinance text is actually the
full ordinance text, not merely whether it is readable.

Judge only the artifact you were given. Do not reconstruct missing language.

Use this decision procedure:

Step 1. Classify the artifact mentally before assigning a label.
Possible forms include:
- full ordinance text
- continuation fragment / excerpt / text with omitted pages
- summary / abstract / digest of an ordinance
- adopting ordinance that points to an external codified text, attached book,
  or separate code and then publishes only a summary or enumeration of new
  matter

Step 2. Map that to completeness.
- If you have strong gap evidence that pages or substantive blocks are absent,
  return `definitely_incomplete`.
- If the artifact is a summary / abstract / digest, return at least
  `likely_incomplete`.
- If the artifact explicitly says the full codified ordinances, attached book,
  or complete text exists elsewhere and what is printed here is only a summary,
  enumeration, or summary of new matter, return `definitely_incomplete`.
- If the artifact reads like a full ordinance text and only has local OCR noise,
  omitted map attachments, missing signatures, or small numbering oddities,
  return `complete_or_nearly_complete`.
- If the only concern is indirect structure suspicion, such as a zone section
  being shorter than neighboring sections or the ordinance stopping at a place
  where you might have expected more, prefer `uncertain`, not
  `likely_incomplete`.

Strong gap evidence means one or more of:
- `[MISSING TEXT]`
- explicit notes that earlier pages, later pages, intervening sections, or
  continuation pages are unavailable
- `continued from page ...` near the start
- `continued on page ...` near the end
- text that clearly begins or ends in the middle of a sentence, section, or
  article
- explicit jumps that clearly omit major ordinance blocks

Hard rules:
- Do not infer incompleteness from omitted zoning maps or referenced attachments
  alone.
- Do not infer incompleteness just because neighboring zones elsewhere in the
  ordinance have more subsections; use the current section's own text.
- A title/body mismatch can be strong evidence when the title says `summary`,
  `summary of ordinance`, `summary of new matter`, `abstract`, `enumeration`,
  or similar.
- If there is no strong gap evidence and no explicit summary/adopting-ordinance
  evidence, you may not return `definitely_incomplete`.

When estimating missing share:
- estimate only missing substantive ordinance body
- do not count trivial OCR corruption
- for summary/adopting-ordinance artifacts, estimate how much of the promised
  substantive ordinance text appears absent from the artifact

Return strict JSON matching the required schema.

Issue ID: {{ISSUE_ID}}

Current extracted ordinance text:

{{ORDINANCE_TEXT}}
